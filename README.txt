Componente: Mattia Bove
Indirizzo repository: 

L'esercizio fa riferimento a "eser.css.3.html" del documento "lweb-part02-XHTML.CSS.2.pdf" 
nella directory delle lezioni.

Nell'esercizio ho usato elementi block-level, come: <h1>, <p>, <table>, <form>...
e elementi inline (<a>, <img>, <strong>).

Per quanto riguarda le CSS ho usato le internal stylesheet.

Ho modificato l'esercizio inserendo alla fine un sondaggio, in cui viene richiesto di inserire e selezionare
alcuni dati. Se non sono compilati tutti i campi, dopo aver schiacciato INVIO, viene visualizzato
un messaggio d'errore (in rosso) con i relativi campi non compilati. Altrimenti
viene visualizzato un messaggio in blu.

Per la form ho usato il metodo post, e il programma cgi è in C (nella directory cgiFile).